# elevm source-hygiene allowlist — consumed by scripts/check-hygiene.sh.
#
# Each non-comment line is "<relpath> <normalised source line>" naming a
# CURRENTLY-COMMITTED occurrence of a forbidden pattern (dbg_trace / sorry)
# under Elevm/ that is deliberately retained. The gate fails on any occurrence
# NOT listed here, so a new stray dbg_trace or sorry cannot enter unnoticed.
#
# Keep a justification comment above every entry. When the underlying code is
# removed, delete its entry (the gate warns about stale entries).

# Deliberate, off-by-default verbose-logging facility. `verbose ()` returns
# false at runtime and is @[never_extract]/@[implemented_by]-protected against
# closed-term extraction (see the doc comment above `verbose` in Basic.lean).
# `cprint` and `Except.print` are its only sinks and both gate on it. Covers
# both occurrences (Basic.lean:1786 in cprint, Basic.lean:1789 in Except.print).
Elevm/Basic.lean dbg_trace msg

# FINDING — flagged, NOT blessed; allowlisted only to keep the gate green while
# a proper fix is tracked separately. Defensive fallback on a
# should-be-unreachable branch of the SHA-256 finaliser: it returns a zero hash
# and announces the impossibility only via dbg_trace (invisible in normal
# operation). This is the "Blake2F lesson" pattern — a silent wrong answer
# masked by a trace. Fix: replace with a loud panic!, or make the 8-word state
# length-indexed so the branch is provably dead, then delete this entry.
Elevm/Hash.lean | _ => (dbg_trace "incorrect number of 32-bit numbers in hash"; 0)

# Not a live `sorry`: a commented-out proof sketch inside a disabled lemma body
# (the Adr'/toNat block in Types.lean), retained as a development note.
Elevm/Types.lean -- sorry
